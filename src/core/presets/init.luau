--!strict
local Object = require("./Object");
local Instance = require("./Instance");
local BasePart = require("./BasePart");
local DataModel = require("./DataModel");
local ScreenGui = require("./ScreenGui");

return {
    Object = Object,
    Instance = Instance,
    BasePart = BasePart,
    ScreenGui = ScreenGui,
    DataModel = DataModel,
}
